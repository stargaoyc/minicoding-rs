# 工具链（2026-08-30 解除日期钉住）：曾钉住 nightly-2026-08-18，因上游
# c656540d(2026-08-21) 在 tokio opaque type 上触发 rustc ICE——
# "unexpected rigid alias in layout_of after normalization"（波及全 workspace 的
# clippy/test）。2026-08-27 构建（e457a7b0d）已验证 ICE 修复，改回滚动 nightly。
# 如再次触发 ICE，回退日期钉住并记录。
#
# ENG-2（2026-08-28 R5 收尾，如实披露）：`rust-version = "1.99"` 是 MSRV 声明
# （Rust 2024 edition 最低要求），实际构建固定 nightly——两者不矛盾但需说明：
# 项目使用 `let chains` 等 2024 edition 语法，1.99 在钉版时刻仍处 nightly/beta，
# 故用 nightly 保证 CI/本地一致可复现。stable 1.99 发布后应切换
# `channel = "stable"`（届时同步删除本注释）。
[toolchain]
channel = "nightly"
