[package]
name = "minicoding-sandbox"
description = "OS 级沙箱驱动（基于 sandbox-run + landlock + libseccomp，主流库不自研）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
minicoding-core = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
camino = { workspace = true }

# 平台重依赖按 target cfg 隔离（见 modules.md §7.4、tech-stack.md §11）
# M0 阶段仅占位：detect_driver() 返回 NoopDriver（T-M0-7）
# M4 起按平台补齐：
# [target.'cfg(target_os = "linux")'.dependencies]
# sandbox-run = "0.43"
# landlock = "0.4.5"
# libseccomp = "0.x"
# libc = "0.2"
#
# [target.'cfg(target_os = "macos")'.dependencies]
# sandbox-run = "0.43"
# libc = "0.2"
#
# [target.'cfg(target_os = "windows")'.dependencies]
# windows = { version = "0.x", features = ["Win32_System_JobObjects", "Win32_Security"] }
